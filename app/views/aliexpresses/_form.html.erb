<%= simple_form_for @aliexpress, :html => { :class => 'form-horizontal' } do |f| %>
  <%= f.input :name, placeholder: 'Nome do Usuário' %>
  <%= error_span(@aliexpress[:name]) %>
  <%= f.input :email, placeholder: 'user@email.com' %>
  <%= error_span(@aliexpress[:email]) %>
  <%= f.input :password, placeholder: 'password' %>
  <%= error_span(@aliexpress[:password]) %>

  <%= f.button :submit, :class => 'btn-primary' %>
  <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                aliexpresses_path, :class => 'btn btn-default' %>
<% end %>
